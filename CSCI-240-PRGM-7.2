#include <iostream>
#include<fstream>
#include<string>
#include<iomanip>
#include<cstdlib>
//symbolic constants
#define lowerbd 1.0
#define upperbd 750.0
#define MAX 150
#define MIN 25
#define PERLINE 10

using namespace std;

//Function prototypes
int buildFromFile(double array[]);
void print( string title, double array[], int numberOfValues );
void sort( double array[], int numberOfValues );

int main()
{
   //Set srand seed
   srand(88);
   
   //Declare arrays and variable
   double fileArray[MAX];
   int array2; //variable
   
   //Call function
   array2 = buildFromFile(fileArray);
   cout << "There are 38 values in the second array " << array2 << endl;
 
   //display array
   print( "File Values: ", fileArray, array2 );
   
   //sort
   sort( fileArray, array2 );
   
   //display array
   print( "Sorted File Values: ", fileArray, array2 );
  
}

int buildFromFile(double array[]) 
{
   
   int i=0;
   double num;
   
   //input file stream variable
   ifstream infile; 
   
   //open file for reading
   infile.open( "nums.txt" );
   
   //File open error check
   if ( infile.fail() ) 
   {
       cout << "nums.txt file not found." << endl;
       exit(-1);
   }
   //Read file data
   while ( infile.open() ) //getting an error here...
   {
       array[i] = num;
       i++;
   }
   return i;
}
